# Grafana admin password. Set on first run; changing requires a Grafana restart.
# Use single quotes if the password contains special characters (@ $ ! etc.).
GF_SECURITY_ADMIN_PASSWORD='choose-a-strong-password'
