# Used by immich-server (DB_PASSWORD) and immich-database (POSTGRES_PASSWORD).
# Generate with: openssl rand -hex 32
# Both variables must be set to the same value.
DB_PASSWORD=change-me
POSTGRES_PASSWORD=change-me
