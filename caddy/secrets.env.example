# Cloudflare API token used for DNS-01 TLS challenge.
# Needs Zone.Zone (read) + Zone.DNS (edit) permissions.
# https://dash.cloudflare.com/profile/api-tokens
CF_API_TOKEN='your-cloudflare-api-token'
